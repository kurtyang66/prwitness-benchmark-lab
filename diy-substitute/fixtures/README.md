# Synthetic fixtures

The matrix creates all JSON and ZIP inputs in memory so no customer artifact,
secret, or PR source is needed. The CLI examples can point at customer-owned
fixtures following the contract in `DIY_BASIC.md` and `DIY_ARTIFACT.md`.
